import React, { useState, useRef, useEffect } from 'react';
import { useInterval } from './useInterval.js';
import {
    CANVAS_SIZE,
    SNAKE_START,
    APPLE_START,
    SCALE,
    SPEED,
    DIRECTIONS
} from './constants.js';

function App() {
    return (
        <div className="App">

        </div>
    );
}

export default App;
